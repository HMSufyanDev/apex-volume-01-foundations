# Exercise 1 — PKR to USD (Randomly, just for practice)

pkr_amount: int = 100000
usd_rate: float = 278.4

usd_amount: float = pkr_amount / usd_rate
print(usd_amount)


# Exercise 2 — Calculate age

birth_year: int = 2003
current_year: int = 2026

age: int = current_year - birth_year
print(age)

# Exercise 3 — Rectangle area
length: float = 10.0
width: float = 5.0

area: float = length * width
print(area)

# Exercise 4 — Celsius → Fahrenheit

celsius: float = 30.0
fahrenheit: float = (celsius * 9 / 5) + 32

print(fahrenheit)
